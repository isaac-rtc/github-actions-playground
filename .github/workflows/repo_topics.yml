name: Create repo topics

on:
  workflow_dispatch

jobs:
  create_topic:
    runs-on: ubuntu-latest

    steps:
      - name: Replace repository topics
        run: |
          curl \
            --request PUT \
            --url "https://api.github.com/repos/${{ github.repository }}/topics" \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${{ secrets.TOPICS_TOKEN }}" \
            --header "X-GitHub-Api-Version: 2026-03-10" \
            --header "Content-Type: application/json" \
            --data '{
              "names": [
                "octocat",
                "atom",
                "electron",
                "api"
              ]
            }' \
            --fail
